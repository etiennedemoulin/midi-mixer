{
  "name": "midi-mixer",
  "description": "soundworks application",
  "authors": [],
  "license": "BSD-3-Clause",
  "version": "2.0.2",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "npm run clean && sass src/clients/styles:.build/public/css && soundworks-build -b",
    "clean": "soundworks-build -D",
    "dev": "npm run build && (concurrently -i -p \"none\" \"npm run watch:inspect server\" \"soundworks-build -b -w\" \"npm run watch:sass\")",
    "start": "node .build/server/index.js",
    "watch": "soundworks-build -p",
    "watch:inspect": "soundworks-build -i -p",
    "watch:sass": "sass --watch src/clients/styles:.build/public/css",
    "lint": "eslint .",
    "electron:dev": "soundworks-electron dev",
    "electron:build": "soundworks-electron build",
    "max:dev": "npm run build && rm -R max/midi-mixer/javascript && mkdir max/midi-mixer/javascript && cp -R .build max/midi-mixer/javascript && cp -R public max/midi-mixer/javascript && cp -R config max/midi-mixer/javascript && esbuild ./src/server/index.js  --bundle --watch --format=cjs --platform=node --external:max-api --outfile=max/midi-mixer/javascript/midi-mixer.cjs",
    "max:build": "esbuild ./src/server/index.js  --bundle --format=cjs --platform=node --external:max-api --outfile=max/midi-mixer/javascript/midi-mixer.cjs"
  },
  "dependencies": {
    "@ircam/sc-components": "^3.0.0-alpha.65",
    "@ircam/sc-utils": "^1.4.0",
    "@soundworks/core": "^4.0.0-alpha.25",
    "@soundworks/helpers": "^1.0.0-alpha.14",
    "@soundworks/plugin-scripting": "^2.0.0-alpha.3",
    "clone-deep": "^4.0.1",
    "esbuild": "^0.24.0",
    "json5": "^2.2.3",
    "jzz": "^1.8.5",
    "lit": "^3.1.3",
    "node-osc": "^9.1.3"
  },
  "devDependencies": {
    "@ircam/eslint-config": "^1.2.1",
    "@soundworks/build": "^1.0.0-alpha.1",
    "@soundworks/create": "^1.0.0-alpha.23",
    "concurrently": "^8.2.2",
    "eslint": "^9.5.0",
    "sass": "^1.77.1"
  }
}
